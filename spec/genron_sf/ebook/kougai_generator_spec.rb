# frozen_string_literal: true

RSpec.describe GenronSf::Ebook::KougaiGenerator do
  it "has a version number" do
    expect(GenronSf::Ebook::KougaiGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
